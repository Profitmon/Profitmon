import { PublicKey, Transaction } from "@solana/web3.js";

// Placeholder for creating escrow transaction
export const createEscrowTx = async (playerWallet: string, amount: number): Promise<Transaction> => {
  // Construct Solana escrow transaction
  return new Transaction();
};
